
import { parseString } from 'xml2js';
import { generateSign, generateRandomStr } from './utility';

// { appId, appSecret, mchId, partnerKey, notifyUrl }
class Wechat {
	constructor(params) {
		this.params = params;
	}

}

export default Wechat;
